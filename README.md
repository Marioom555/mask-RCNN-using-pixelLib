The code in the Jupyter Notebook uses the following tools:

TensorFlow (tensorflow) - Used for deep learning model operations.
PixelLib (pixellib) - Used for instance segmentation with Mask R-CNN.
OpenCV (opencv-python, cv2) - Used for video processing and displaying segmentation results.
